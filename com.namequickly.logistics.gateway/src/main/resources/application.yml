server:
  port: 19091

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:19090/eureka/

spring:
  application:
    name: gateway-service

  cloud:
    gateway:
      routes:
        - id: hub-service
          uri: lb://hub-service
          predicates:
            - Path=/hub/**
          filters:
            - JwtAuthenticationFilter

        #?? ????? ?????

# gateway-service ?? JWT ??? ??? ??? ??? ???? ??????? ? ????? ???? ?????.